# golden/ — committed expected outputs (MW-J4)

Byte-compared by e2e tests. Each file lands with the feature that produces
it (ready-alpha.json with PLAN 0.8, lint-broken.json with 0.9, …).
Regenerate ONLY via `--bless` + a reviewed git diff — never silently.
