//! meshwork — task graph as markdown-in-git, queried with SQL, no database.
//!
//! Bootstrap stub: the CLI surface is normative in DESIGN-meshwork.md §6 and
//! lands milestone by milestone per PLAN-meshwork-build.md. Until M0 items
//! exist, running the binary points at the plan instead of pretending.

fn main() {
    eprintln!("meshwork: bootstrap only — no verbs yet. Dev starts at PLAN-meshwork-build.md (see Position line).");
    std::process::exit(2);
}
