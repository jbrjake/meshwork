# meshwork store config (hand-editable; DESIGN §1).
# alias prefixes every task ID — pick it before the first `add`;
# IDs embed it forever.
alias = "mw"
default_author = "Jon Rubin"

# Cosmetic names for category depths (MW-B8) — uncomment to taste:
# [hierarchy]
# levels = ["saga", "epic", "sprint", "story"]
