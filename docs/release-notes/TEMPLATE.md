# meshwork vX.Y.Z

One paragraph: the theme of the release and why a user should care. Lead with user impact — new features, fixes that change what the tool does or says. Internal refactors, store chores and task ids do not belong here. Plain English; an agent drafts, the owner lands the prose.

## the headline change

What it does for the user, in the user's terms. Show the command or the line of output when that says it faster than prose.

## smaller additions

- One line each, verb first.

## fixes

- What was wrong, what happens now.

## getting it

darwin arm64, linux arm64/x86_64, windows x86_64. Pin: put `vX.Y.Z` in `.meshwork-version`; install to `~/.meshwork/versions/vX.Y.Z/` (see the meshwork adoption skill).
